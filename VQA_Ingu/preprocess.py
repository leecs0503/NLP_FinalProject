import os
import argparse
import numpy as np
import torch
from util import text_helper

def main(args):
    pass

if __name__=='main':
    parser = argparse.ArgumentParser()

    # TODO : argument 추가

    args = parser.parse_args()

    main(args)